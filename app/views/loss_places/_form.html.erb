<%= form_with(model: loss_place, local: true) do |form| %>
  <% if loss_place.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(loss_place.errors.count, "error") %> prohibited this loss_place from being saved:</h2>

      <ul>
        <% loss_place.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :latitude %>
    <%= form.text_field :latitude %>
  </div>

  <div class="field">
    <%= form.label :longitude %>
    <%= form.text_field :longitude %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
